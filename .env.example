# Local development only. Replace these values before any shared deployment.
POSTGRES_USER=mlflow
POSTGRES_PASSWORD=claimguard-postgres-dev
POSTGRES_DB=mlflow

MINIO_ROOT_USER=claimguard-minio
MINIO_ROOT_PASSWORD=claimguard-minio-dev-password
MLFLOW_BUCKET=mlflow-artifacts
